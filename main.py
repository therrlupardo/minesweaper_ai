from Minesweeper.BrowserHandler import BrowserHandler
from MLSolvers.Model import Model

if __name__ == "__main__":
    BrowserHandler('m', 'm')
    # BrowserHandler(size, method)
    # 's' --> small v simple_solver
    # 'm' --> medium v ml_solver
    # 'l' --> large v logic_solver

    # Model()
